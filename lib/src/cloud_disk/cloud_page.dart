import 'package:flutter/material.dart';

class CloudDiskPage extends StatelessWidget {
  const CloudDiskPage({super.key});

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Container(
      child: Text("hello"),
    );
  }
}
